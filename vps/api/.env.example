# MQTT
MQTT_HOST=127.0.0.1
MQTT_PORT=1883
MQTT_USER=api
MQTT_PASS=api_pass_change_me

# HTTP
HTTP_PORT=3000
API_TOKEN=champ_default_token_change_me

# SQLite
DB_PATH=/opt/champ/data/champ.db

# 域名（生成 CORS / 快照 URL 用）
DOMAIN=localhost
